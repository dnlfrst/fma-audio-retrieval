import styled from "styled-components";

const RotatedLabel = styled.span`
  color: grey;
  font-size: small;
  margin-right: 10px;
  transform: rotate(-180deg);
  writing-mode: vertical-lr;
`;

export default RotatedLabel;
